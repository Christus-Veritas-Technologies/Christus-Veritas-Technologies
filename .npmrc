# Hoist all @prisma/* packages to root node_modules so the generated Prisma
# client (which requires @prisma/client-runtime-utils at runtime) can resolve
# them in pnpm's strict-isolation workspace.
public-hoist-pattern[]=@prisma/*
