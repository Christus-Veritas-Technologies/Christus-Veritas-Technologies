# Auth Package Environment Variables
# These variables should be set in the root .env or apps/server/.env

# JWT Configuration
# Secret key for signing and verifying JWT tokens
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# JWT token expiration times
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# Database
# PostgreSQL connection string (used by @repo/db package)
DATABASE_URL="postgresql://user:password@localhost:5432/christusveritas?schema=public"
